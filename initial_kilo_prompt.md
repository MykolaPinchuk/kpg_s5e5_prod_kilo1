### Initial prompt to Kilo Code [only for human users]

The goal of this project is to deploy trained ML model locally. We want to do it iteratively starting from a very simple POC-like deployment and progressing towards deployment closely simulating production environment. To derisk this process, we want to divide this into several iterations. Human will validate and approve each iteration.

Everything required for deployment should have been provided by DS agent in a folder 'input_files_from_DS_agent'.

Initial planning is likely to be the hardest part of this project. Do not proceed until human approved this plan.